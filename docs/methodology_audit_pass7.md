# waterfall-py: Methodology — Seventh-Pass (Re-Audit) Findings

> **STATUS: ACTIVE.** Hostile seventh-pass audit of the *pass-6-resolved* methodology (`docs/methodology.md`, resolution commit `e41a32a` on `methodology-review-pass7`, branched from `methodology-review-pass6`) in a fresh, adversarial session. Lineage: skeleton `53294fc` → audit #1 (2 CRIT / 5 HIGH / 6 MED / 5 LOW) → … → audit #6 `methodology_audit_pass6.md` (`14c519c`): **NO-GO, 0 CRIT / 1 HIGH / 2 MED / 2 LOW** → resolution #6 `e41a32a` → **this pass.**
>
> **Basis of review: the live text of `docs/methodology.md` at `e41a32a`, read line-by-line, plus an independent re-derivation of the cash model over FIVE period types against the arithmetic and cross-references — NOT the STATUS-block prose, NOT the commit message, and NOT the author's own pre-handoff self-audit.** The dominant pattern of this project's history: five of six prior resolutions fixed the named finding and shipped (or left) a *new self-inflicted contradiction between two sections describing the same cash mechanic* (citation antipattern; reserve-in-CFADS; ECF↔ladder; false "sum-to-CFADS" identity; closed four-source set). The pass-6 resolution made two structural moves intended to end the pattern: (1) it generalized the cash-conservation identity to a **per-period ledger identity** ("all modeled sources = all modeled uses, enumerated from the ledger, not a hardcoded list"), which is a *structural* fix to the recurring omitted-source bug class rather than another additive patch; and (2) the author ran an internal hostile self-audit and fixed a Scope-vs-cash-trap contradiction (the "SPV tax distributions permitted at 6c during lock-up" carve-out). This pass has two jobs: **(1) independently confirm both structural moves rather than trusting them, and (2) attack everything the cash-model rewrite did not touch — including the *other* route cash can reach equity.**

**Result: 0 CRITICAL, 0 HIGH, 1 MEDIUM, 2 LOW.**

## VERDICT: **GO.**

GO requires 0 CRITICAL and 0 HIGH. This pass finds **0 CRITICAL and 0 HIGH.** The methodology is **cleared for the v0.x build-plan stage.** The one MEDIUM and two LOWs below are real and should be cleaned up in the build-plan revision, but none gates GO and none corrupts any coverage arithmetic or fires a spurious assertion in any of the five period types traced.

I am stating this plainly, against six prior NO-GOs, because the two structural changes actually did what they claimed:

- **The recurring omitted-source bug class is closed *structurally*, not patched.** Prior passes each found "a cash source modeled elsewhere is missing from the identity's fixed list." The pass-6 rewrite (line 171) replaced the fixed list with *"the engine builds both sides from the actual per-period ledger, not from a hardcoded list … the following are illustrative of what the engine currently models, not a closed set."* You cannot omit a ledger entry from an identity that enumerates the ledger. I re-derived the identity over **five** period types (surplus, DSRA-draw distress, maturity/release, mid-life equity cure, revolver draw) for both a CRE and a PF deal; all five balance and none fires spuriously (traces below). The pass-6 HIGH is genuinely dead, and — unlike pass-5's fix — it is drift-proof against the *next* modeled inflow, too.
- **The author's self-caught Scope↔cash-trap fix is real and consistent.** Scope (line 20) and the Cash-trap/lock-up paragraph (line 86) now state the identical rule: SPV tax distributions are out of v0.x scope, are **not** funded from the waterfall, and there is **no** lock-up carve-out. The "permitted at 6c during lock-up" language pass-6 MED-1 flagged is gone from both sections; no residual "sit ahead of lock-up" language survives in Scope. Confirmed independently, not trusted.

The remaining defect is the *seventh* instance of the house pattern — but at MEDIUM, and it is the one place the cash-model rewrite's focus stopped: the pass-6 rewrite of line 86 *strengthened* a blanket "no cash reaches equity under a trap" claim for the operating ladder while leaving the **separate proceeds path** (line 88) with an un-gated "then to equity" leg. It mis-specifies the discretionary/residual-equity region only, in a narrow edge case, and is economically defensible under one reading — hence MEDIUM, not HIGH.

---

## CRITICAL

*None.* No finding this pass touches a coverage numerator (DSCR/LLCR/PLCR arithmetic is unaffected), silently corrupts a number, or produces an uncaught failure. The single MEDIUM concerns cash routing in the junior/residual-equity region *after* senior and all debt are satisfied in priority order — it cannot shortchange a lender.

---

## HIGH

*None.* The pass-6 HIGH (closed source set) is resolved structurally and re-verified over all five required period types; see the re-derivation and the pass-6 verification section below. No new HIGH was introduced by the resolution.

---

## MEDIUM

### MED-1 (residual, self-inflicted by the pass-6 MED-1 rewrite, same house pattern): The "no cash to equity under a trap" guarantee is asserted blanket at line 86 but is only *established* for the operating ladder — the separate proceeds path (line 88) has an un-gated "then to equity" leg, so cash can still reach equity during an active cash-trap in the proceeds-exceed-remaining-debt edge
- **Section:** `## Waterfall Priority` — Cash-trap / lock-up behavior (line 86, **rewritten this resolution**) vs. the Separate proceeds path (line 88), step 7 (line 84), and Reserves → DSRA Release (line 66).
- **The claim (line 86, strengthened this resolution):** *"Retained ECF is then 0, so **steps 6 and 7 receive no cash** — no junior use, distribution, or **residual-to-equity** occurs while the trap is active … This is why lock-up-blocked cash cannot leak to step 7 equity: under a trap there is no retained cash to leak."* The stated *mechanism* of the guarantee is "there is no retained ECF." That mechanism covers **only the operating ladder** (steps 1–7), whose equity leg (step 7) is fed by retained ECF.
- **Why it's wrong (a second, un-gated route to equity exists):** Line 88 defines a **separate proceeds path**, explicitly *"not part of the operating-cash ladder,"* for reserve releases and event-driven proceeds: *"applied to outstanding debt in priority order, **then to equity** — they are not swept as ECF (consistent with the CFADS-only scope of steps 1–7 and MED-1)."* This leg has **no lock-up/cash-trap gate.** Its "consistent with … MED-1" self-claim is about *not being swept as ECF*, not about equity gating — so line 88 routes cash to equity by a mechanism line 86's "no retained ECF" reasoning never addresses. Line 66 confirms the route is live during operations: a DSRA (or other reserve) *"released at final maturity (or per a trigger)"* is *"applied on the separate proceeds path — to outstanding debt in priority order, then to equity residual."*
- **Worked edge (constructible, in-scope):** Active trap-tier covenant breach (DSCR below the trap threshold, so the trap is on) with a **small senior debt stub of 5 remaining** and a **large reserve releasing 20** on its trigger period. Separate proceeds path: 5 repays the stub (debt → 0), **15 flows to equity residual** — while the trap flag is set for that period. Line 86's blanket sentence ("no residual-to-equity occurs while the trap is active") reads as violated. The cash-conservation identity still *balances* (release 20 as source; 5 debt + 15 equity as the two use legs = 20), so no assertion fires — this is a routing/spec contradiction, not an arithmetic one.
- **The mitigating reading (why MEDIUM, not HIGH):** proceeds hit *outstanding debt in priority order first*; equity is reached **only** after **all** debt is fully retired. At the instant the 15 flows, debt = 0, so no lender is shortchanged, and one can argue the trap is economically moot once there is no debt left to protect. Under that reading the behavior is *correct* and only line 86's categorical prose over-reaches. Under the stricter reading (the trap flag is still set for the period, so "no cash to equity" should hold literally), it is a contradiction. Either way it mis-specifies only the residual-equity region and cannot mis-allocate cash between debt classes — the same severity logic pass-6 used to rate its own step-6 lock-up finding MEDIUM.
- **Direction:** State explicitly whether the **separate proceeds path's equity leg is also blocked/held under an active lock-up** (parallel to step 7), or scope the edge out in one sentence: *"once all debt is retired the trap is moot; a reserve release or event-proceeds residual arising after full debt retirement is not trapped."* Either resolves the tension. Do not leave line 86's blanket "no residual-to-equity while the trap is active" standing next to an un-gated "then to equity" at line 88.

---

## LOW

- **LOW-1 (cosmetic, prose ambiguity introduced by the pass-6 ledger rewrite) — The illustrative *uses* list disambiguates facility *draws* but not facility *repayments*, inviting the exact double-count the draws-disambiguation was added to prevent.** The identity's sources side (line 172) lists *"facility draws — revolver/delayed-draw fundings,"* and the principal-trace note (line 175) carefully distinguishes *"facility draws (tranche fundings) — distinct from DSRA reserve draws."* But the uses side (line 173) lists *"facility repayments"* as a use **alongside** *"Waterfall Priority steps 1–7,"* with no note on how a facility repaid via the step-5 ECF sweep or step-2 scheduled amort is prevented from being counted **both** as a step (1–7 use) **and** as a "facility repayment" use. The ledger-based construction (line 171) resolves this in the actual assertion (each cash movement is one tagged ledger entry, counted once), so no double-count fires — but the prose invites precisely the confusion the draws-vs-reserve-draws note exists to kill. Add a parallel one-line disambiguation: *"'facility repayments' here means financing outflows on a facility tranche, counted once — a repayment effected via the step-5 sweep or step-2 amort is that step, not a second use."*
- **LOW-2 (cosmetic, stale lineage numbering) — The STATUS block and Next-steps carry inconsistent, stale re-audit numbering.** Line 3 reads *"PENDING FRESH RE-AUDIT #6"* though this *is* the post-pass-6 resolution whose next gate is the **seventh** audit (this document). Next-steps line 245 still reads *"Fresh-session hostile re-audit **(pass 5)** of THIS resolved methodology"* — a leftover never advanced past the pass-4→5 revision; it should read pass 7. Neither affects any mechanic, but a defensible-output document whose own audit-lineage counter is wrong invites a "which version did your expert actually review?" cross. Advance line 3 to "PENDING FRESH RE-AUDIT #7 (this pass)" and line 245 to "pass 7."

---

## Independent cash-model re-derivation over FIVE period types (traced, not trusted)

CRE vs. PF differ **only** in the CFADS build (line 151: PF = revenues − opex − cash tax − maint. capex; line 152: CRE = NOI − maint. capex − TI/LC). Both feed the **identical** ladder (lines 78–84) and the identical ledger identity (line 171), so the tie-out arithmetic is deal-type-agnostic; I trace each period type once and note the CRE/PF equivalence. The identity (line 171): **Σ all modeled sources = Σ all modeled uses**, or `WaterfallImbalanceError`. Ladder: 1 senior fees; 2 senior DS (shortfall → DSRA draw, then equity cure); 3 required reserve funding; 4 mezz DS; 5 ECF sweep (ECF = residual after 1–4); 6 junior 6a/6b/6c; 7 residual to equity. Separate path: reserve releases + event proceeds → debt in priority, then equity (line 88).

**(a) Surplus.** CFADS 100; fees 3; senior DS 40; req. reserve (step 3) 10; mezz DS 20 → ECF = 100−3−40−10−20 = **27** (= residual after steps 1–4 ✓). Sweep band 50% → sweep 13.5; retained 13.5 → step 6 (6a 5 / 6b 8 / 6c 0.5) = 13.5; step 7 = 0.
- Identity: sources = CFADS 100. uses = 3+40+10+20+13.5+13.5+0 = **100.** ✓
- Reserve roll-forward (line 177): opening + 10 (step 3) + 8 (6b) − 0 − 0 = opening + 18; 10 and 8 are both *uses* in the identity, no double-count. ✓

**(b) DSRA-draw distress (CFADS < senior DS).** CFADS 30; fees 3; senior DS 40; DSRA opening 20. Step 1 → 27 left; step 2 needs 40, draws **13** from DSRA, pays 40; steps 3–7 = 0.
- Identity: sources = CFADS 30 + reserve draw 13 = **43.** uses = 3 + 40 = **43.** ✓ Does **not** fire spuriously — the pass-5 HIGH stays fixed.
- Reserve roll-forward: opening 20 + 0 + 0 − 13 − 0 = **7.** ✓

**(c) Maturity with reserve release.** CFADS 50; fees 2; final senior DS 48; mezz 0; DSRA releases **20** at maturity. Steps 1–2 consume 50; ECF = 0; steps 5–7 = 0. Release 20 → separate proceeds path (debt retired, then equity residual).
- Identity: sources = CFADS 50 + release 20 = **70.** uses = (steps 1–7: 2+48 = 50) + (separate application of release 20) = **70.** ✓ Does **not** fire at release.
- Reserve roll-forward: opening 20 + 0 + 0 − 0 − 20 = **0.** ✓

**(d) Mid-life equity cash-injection cure — the period pass-6 HIGH-1 said would throw or corrupt CFADS.** CFADS 30; fees 3; senior DS 40; DSRA exhausted (opening 0). Sponsor elects a **cash-injection cure of 13** to make the senior coupon. Step 1 pays 3 → 27; step 2 needs 40, has 27 CFADS + 13 cure = 40, pays 40.
- Identity: sources = CFADS 30 + **cure injection 13** = **43.** uses = 3 + 40 = **43.** ✓ **Balances — no spurious `WaterfallImbalanceError`.** Under the pass-6 general identity the cure is a *source* (line 172: *"equity contributions and cash-injection cures"*), not folded into CFADS.
- **CFADS not contaminated:** CFADS stays **30** (line 120: cash-injection cure *"does not contaminate CFADS, which stays operating-only"*; line 79 enumerates the cure as a step-2 source; line 120 as a source in the identity). Cure cash is the source; senior DS at step 2 is the matched use. ✓
- Capital-account roll-forward (line 179): opening + contribution 13 − 0 = opening + 13 — a *different* ledger (equity balance), no double-count with the cash identity. ✓

**(e) Revolver / delayed-draw facility draw.** CFADS 30; fees 3; senior DS 40; user-scheduled revolver draw **13** funds the shortfall. Step 1 → 27; step 2: 27 CFADS + 13 draw = 40, pays 40. Draw increases the revolver tranche balance (line 52, line 175).
- Identity: sources = CFADS 30 + **facility draw 13** = **43.** uses = 3 + 40 = **43.** ✓ Balances — no spurious error. Facility draw is a *source* (line 172), and per line 52 *"the drawn cash enters the ledger and funds the use it finances; it increases the tranche balance in the principal trace. Not folded into CFADS."*
- **CFADS not contaminated:** CFADS stays **30**; the draw is a separate financing source, the senior DS at step 2 is the matched use. ✓ (Per line 79 the engine does not *autonomously* draw the revolver to cover a shortfall — draws are user-scheduled — but a scheduled draw in the period ties out identically.)
- Principal trace (line 175): opening + facility draw 13 − 0 − 0 − 0 = opening + 13. ✓

**Conclusion of the re-derivation:** the ledger identity and the two roll-forwards (reserve, capital-account) are correct and mutually consistent across **all five** required period types for **both** deal types, with no double-count and — critically — **no spurious `WaterfallImbalanceError` in the cure or revolver-draw periods** (the two the pass-5 three-scenario check never exercised and pass-6 flagged as HIGH). For (d) and (e) specifically: the cure/facility cash is a **source** on one side and its funded use (senior DS at step 2) is on the other, and **CFADS is not contaminated** — it remains operating-only per its definition in both. The pass-6 HIGH-1 dilemma (spurious error vs. definitional corruption) no longer exists.

## Verification of pass-6 resolutions (each checked against the live `e41a32a` text, not the prose or commit message)

- **Pass-6 HIGH-1 (closed four-source set omitted cure/facility inflows) — RESOLVED, structurally.** ✓ Line 171 replaces the fixed list with a per-period ledger identity, both sides *"enumerated from the ledger at assertion time,"* the enumerated items *"illustrative … not a closed set."* The illustrative sources (line 172) now explicitly include *"equity contributions and cash-injection cures"* and *"facility draws — revolver/delayed-draw fundings,"* each *"not folded into CFADS."* Cures/draws appear as **sources** with **named uses** (cure → the step it funds, line 79/120; facility draw → the use it finances, line 52) and are **not** in CFADS. Re-derived clean on the cure period (d) and revolver period (e). The identity is now general and drift-proof.
- **Pass-6 MED-1 (step-6 lock-up contradiction; SPV-tax carve-out; Scope↔cash-trap agreement) — RESOLVED.** ✓ Line 86: a trap forces the step-5 sweep to **100%**, retained ECF = 0, **steps 6 and 7 receive no cash**; the *"permitted at 6c during lock-up"* carve-out is **removed**. Scope (line 20) and the cash-trap paragraph (line 86) now state the **same** rule — SPV tax distributions out of scope, not funded from the waterfall, no lock-up carve-out — and I confirm **no residual "sit ahead of lock-up" language in Scope.** The self-audit's catch is genuine and consistent. *(The one remaining seam — the separate proceeds path's un-gated equity leg — is a distinct region from the step-6/7 carve-out this finding closed; see MED-1 above.)*
- **Pass-6 MED-2 (output completeness / missing fee column) — RESOLVED.** ✓ The period table (line 185) now carries **`fees` (step-1 senior fees & admin)** plus a column for **every** non-CFADS source (`facility_draws`, `equity_contributions`, `reserve_draws`, `reserve_releases`, `proceeds_applied`), and the completeness claim is now true: I mapped every identity source and every use (steps 1–7) to a column — all present or derivable. The audit log (line 186) now enumerates *"fees paid," "facility draw taken," "equity cure/contribution applied."* Reconciled with Fee Modeling's *"shown separately"* (line 57). A reviewer can tie the identity out from the table alone.
- **Pass-6 LOW-1 (ECF-base non-deduction prose dropped 6b) — RESOLVED.** ✓ Both non-deduction lines now name discretionary reserve top-ups: line 90 (*"growth capex, **discretionary reserve top-ups**, and permitted distributions … not deducted in the ECF base"*) and line 113 (*"Growth/discretionary capex, **discretionary reserve top-ups**, and permitted distributions are NOT deducted"*).
- **Pass-6 LOW-2 (DSRA sizing said unqualified "debt service") — RESOLVED.** ✓ Line 68: *"N months **senior** debt service (DSRA — sized on the same base it is drawn against and the default DSCR tests)"* — aligned with the senior-only draw trigger (line 64) and the default senior DSCR denominator (line 95).

## Audit outside the cash model (Interest, Principal, Fees, Cure Rights, Default/Remedies, Call Protection, Guardrails, Limitations, Definitional Basis, Required Parameters, Release Process)

- **External-authority claims outside the removed-ledger — NONE load-bearing.** ✓ Grep for Greene/SIFMA/Moody's/LSTA/S&P/MBA/ISDA/ICMA/CREFC/Yescombe returns only: the Definitional-Basis *removal* list (line 222, historical, "not to be reintroduced"); the disclosed **non-load-bearing** Yescombe *further-reading* reference (line 223, explicitly *"the engine's LLCR/PLCR correctness rests on the inline formulas … not on this text"*); ISDA/ICMA/AFB named at line 39 only as the *reason* ACT/ACT was dropped (not relied on); and SIFMA/Fed named at lines 43/164 only as **calendar** names (operational, not methodology authority). No mechanic is attributed to a published standard. Clean.
- **Deferred / out-of-scope features referenced as in-scope — NONE.** ✓ B-piece, preferred equity (line 30), sculpted-to-DSCR (lines 49–50, 213), OID (line 56, 212), defeasance (line 145), multi-currency (line 33), ACT/ACT (line 39, 214), purchase options (line 32), A/B notes (line 22) are each marked deferred/out and each appears in Limitations (lines 205–215) — none is invoked as available. The `accordion` (line 30) is listed as a supported senior facility (a form of delayed-draw commitment increase), consistently with the facility-draw mechanic; not contradicted. SPV tax distributions (line 20) are out of scope *and* the "supplied as ordinary distribution inputs" handling does **not** re-import the phantom-income calc — consistent with Limitations line 204.
- **Dangling cross-references — NONE material.** ✓ "see Triggers below" (line 66 → 69), "see Cash Sweep Mechanics" (line 82 → 107), "see Mandatory Prepayments" (line 82 → 125), "see below"/"Separate proceeds path" (line 84 → 88), "see Reserves" (line 64), "see Scope" (line 86 → 20), "(Validation Tests)" (lines 52/120 → 167) all resolve. The scattered `HIGH-n / MED-n / LOW-n` tags are historical provenance labels, not section pointers. The only numbering defects are the **stale re-audit counters** at lines 3 and 245 — LOW-2, cosmetic.
- **Metric / assertion that fires spuriously — NONE found beyond the five cash traces.** ✓ Division-by-zero (line 100): a zero denominator (DSCR debt-service = 0, or LLCR/PLCR/LTV balance = 0 after full repayment) reports **n/a**, never ∞/0/raised — does not fire spuriously post-repayment. PLCR/LLCR-project-life horizon is **PF-only** (line 97); CRE does not compute PLCR, so no spurious PLCR on a CRE deal. DSCR default denominator = senior-only (line 95); the *"mirroring the ECF base"* parenthetical is correctly attached to the **total-DSCR variant** (senior+mezz), matching the ECF base's senior+mezz "debt service" (line 113) — no mismatch. Covenants are not tested before period 0 (lines 9, 102) — no spurious construction-period breach. Reserve triggers may not reference un-modeled state such as occupancy (line 69) — no assertion can depend on a field the period table does not carry.
- **Interest / Principal / Fees / Cure / Default / Call / Guardrails — consistent.** ✓ Accrual begins at `operations_start_date`, not `deal_close_date`; `deal_close_date` used only for the close source/use tie-out (line 44) — consistent with the operations-only horizon (line 9) and the required-parameter constraint `operations_start_date >= deal_close_date` (line 162). SIFMA (fixings + lookback) vs. Fed (payment/period) calendars kept strictly separate, every date on exactly one calendar (line 43). Make-whole spread is a **required input with no default** (line 144). Breach/default output carries the "mechanical test result" label adjacent to every flag, and bankruptcy/cross-default/judgment are **user-supplied event inputs**, never engine-asserted (lines 105, 134, 137, 194). Two-layer defensibility (line 195) correctly names the numeric audit-assertion layer as primary. No credit-judgment or legal-conclusion language leaks into output.
- **Release Process — consistent, no methodology impact.** ✓ CI-driven publish, OIDC Trusted Publisher before first publish, SHA-pinned actions, dual-import shim, methodology bundled in wheel (lines 227–232). No mechanic depends on it.

---

## Hostile-Reviewer Simulations

### Former OCC CRE Examiner (30 min)
- **~2 min:** "Last round I broke you on the cure. I'm running it again: 0.75x quarter, DSRA already dry, sponsor wires the shortfall to hold the senior coupon. … Your conservation check lists the cure as a *source* now and balances at 43 = 43 instead of throwing, and your CFADS stayed operating-only at 30. That's the SR 11-7 exception from last round — **cleared.** And you didn't just add 'cure' to a list, you made the whole identity read off the ledger, so the *next* inflow won't trip it either. Good."
- **~11 min:** "Revolver next. Draw 13 to cover the gap — source side, senior DS the use, CFADS untouched, balances. Cleared. Your table even has a `facility_draws` column and a `fees` column now; I can tie the period out without your engine. Fine."
- **~19 min:** "Here's my one push. Your headline says *no cash reaches equity while a trap is on*. But your reserve-release path pays debt then equity with no trap gate. Give me a breached quarter with a 5 debt stub and a 20 reserve release — 5 kills the stub, 15 lands on equity, trap flag still set. Explain that." *(Examiner accepts the "debt's fully repaid, trap's moot" answer but wants it written down.)* "Say so in the document. Right now your blanket sentence and your proceeds path disagree on paper. That's a finding, not a stop — you're not misallocating my collateral, the debt got paid first. Write the carve and it's clean."
- **~27 min:** "Everything else — calendars, coverage denominators, the n/a on zero balance — is where I want it. This one's fundable once the trap sentence is squared with the proceeds path."

### Counterparty Litigation Counsel (30 min)
- **~4 min:** "Three rounds I've had a self-check contradiction to put on a demonstrative — the distribution priority, the distress tie-out, then the cure. This version, the cure quarter *produces numbers* and doesn't gut its own CFADS definition to do it. I lose that cross. The 'primary defensibility mechanism' finally survives its own worst case."
- **~14 min:** "I have one thread left, and it's thinner. Your document says in bold that under a lock-up *no residual reaches equity*. Then your proceeds path says releases go to debt 'then to equity' with no lock-up mentioned. On the right facts — trap on, reserve releasing bigger than the debt left — your own text sends money to my client's equity during a covenant lock-up. Your expert will say 'the debt was fully repaid first, so it's fine,' and honestly that's a good answer — but it's *his* answer, not the document's. The document contradicts itself. That's a paragraph I get to read to the jury slowly."
- **~23 min:** "Your legal-event and breach-labeling language is clean — mechanical test result, contractual consequence, bankruptcy is a user input. No exposure there, same as last two rounds. My only exhibit now is the trap-versus-proceeds sentence, and it's a MEDIUM, not the smoking gun I've had before. Square those two sentences and I've got nothing."

### Senior Project Finance MD (30 min)
- **Immediately:** "You did the thing I asked. You didn't bolt 'cure' and 'revolver' onto a list — you made the conservation identity read the ledger, so it's exhaustive by construction. On a PF deal the sponsor equity cure and the working-capital revolver are both live cash, and both now show up as sources with the coupon as the use, CFADS untouched. That's the whole fix, done at the root. Credit."
- **~9 min:** "And you caught your own Scope-versus-cash-trap contradiction before handing it to me — the tax-distribution carve-out that couldn't be funded under a 100% sweep. It's gone from both sections and they read identically now. That's the discipline I want: audit your own cash model before you ship it."
- **~17 min:** "One seam left, and it's the cousin of the one you *did* fix. You slammed the step-6/7 equity door under a trap — good — but the *reserve-release* door beside it still opens to equity with no trap gate. In a low-DSCR quarter with a big reserve rolling off against a small debt tail, cash lands on equity while the covenant's tripped. I buy that once the debt's fully gone the trap doesn't matter — but say that in the document, or gate the release leg like you gated step 7. Don't leave the blanket promise next to the open door."
- **~24 min:** "That's a MEDIUM and two housekeeping items — your own audit counter still says 'pass 5' in the next-steps, tidy that. Clocks, calendars, PF-only PLCR, the coverage denominators, the labels — all where I want them. **This is a GO.** Fix the release-leg sentence in the build-plan revision, but you're cleared to scope the v0.x build. You finally closed the pattern at the root instead of one mechanism at a time."

---

*Audit basis: pass-6-resolved methodology commit `e41a32a` (`methodology-review-pass7`, branched from `methodology-review-pass6`), reviewed against the live `docs/methodology.md` text line-by-line and an independent arithmetic re-derivation of the cash model over **five** period types (surplus, DSRA-draw distress, maturity/release, mid-life equity cure, revolver draw) for both a CRE and a PF deal. The author's pre-handoff self-audit was **not** trusted; its Scope↔cash-trap fix was independently re-verified and confirmed consistent (no residual "sit ahead of lock-up" language in Scope), and the general ledger identity was re-derived rather than assumed. Prior audits' external-citation removals re-confirmed (grep: no reintroduced load-bearing authority beyond the disclosed non-load-bearing Yescombe further-reading reference). No code reviewed — methodology-only per working principle #1.*

***GO/NO-GO: GO*** *(0 CRITICAL, 0 HIGH, 1 MEDIUM, 2 LOW). The central question of this pass — did the pass-6 resolution finally end the self-inflicted-contradiction pattern at the root — answers: **yes, on the cash model.** The general ledger identity closes the recurring omitted-source bug class **structurally** (not additively), and re-derives clean over all five required period types with no spurious `WaterfallImbalanceError` in the cure or revolver-draw periods and no CFADS contamination; the author's self-caught Scope↔cash-trap fix is genuine and consistent. The pattern recurs a seventh time only at **MEDIUM** and only in the one region the cash-model rewrite did not touch — the separate proceeds path's un-gated equity leg versus line 86's blanket "no cash to equity under a trap" — plus two cosmetic LOWs (facility-repayment prose ambiguity; stale re-audit numbering). None gates GO. **The methodology is internally consistent on its cash mechanics and is cleared for the v0.x build-plan stage.** Fold the MED-1 clarification and the two LOWs into the build-plan revision; they do not require another full re-audit gate before code, though the MED-1 sentence should be settled before the cash-trap path is implemented.*
